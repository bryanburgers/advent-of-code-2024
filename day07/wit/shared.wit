package aoc2024:day07;

interface solver {
    record calibration-equation {
        test-value: u64,
        parameters: list<u64>,
    }
    
    solve-a: func(input: list<calibration-equation>) -> u64;
    solve-b: func(input: list<calibration-equation>) -> u64;
}